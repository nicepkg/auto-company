export default function HomePage() {
  return (
    <main style={{ padding: 24 }}>
      <h1 style={{ marginTop: 0 }}>Security Questionnaire Autopilot</h1>
      <p>
        Hosted workflow is active via API routes under <code>/api/workflow/*</code> with
        hard gates for citations, approval, and pilot pricing floor.
      </p>
    </main>
  );
}
